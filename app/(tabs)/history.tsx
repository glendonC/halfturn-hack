import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export default function HistoryScreen() {
  return (
    <ScreenPlaceholder
      title="History"
      purpose="Local session history — past drills, cue counts, and (later) verified metrics stay on-device."
    />
  );
}
