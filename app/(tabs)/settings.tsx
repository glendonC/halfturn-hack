import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export default function SettingsScreen() {
  return (
    <ScreenPlaceholder
      title="Settings"
      purpose="Defaults for duration, intensity, cue mix, and voice — persisted locally for field sessions."
    />
  );
}
